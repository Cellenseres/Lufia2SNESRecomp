bank = 00

auto_vectors

# The battle callback dispatcher at $05:8F1B constructs a dynamic call with
# PEA/PHA/RTS. Splitting that synthetic frame across AOT and LLE corrupted the
# enclosing $85:8DC5 return in the earlier profile-promoted build. Keep this
# exact boundary authoritative LLE when reviewed CFG roots are enabled.
force_lle 058F1B
